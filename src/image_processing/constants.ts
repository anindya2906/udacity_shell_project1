import path from "path";

const full_image_path = path.join("assets", "full");
const thumb_image_path = path.join("assets", "thumb");
const root_path = path.join(".");

export default { full_image_path, thumb_image_path, root_path };