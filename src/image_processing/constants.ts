import path from 'path';

const fullImagePath = path.join('assets', 'full');
const thumbImagePath = path.join('assets', 'thumb');
const rootPath = path.join('.');

export default { fullImagePath, thumbImagePath, rootPath };
